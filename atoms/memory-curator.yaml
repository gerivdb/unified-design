---
name: memory-curator
description: "Gestion de la mémoire longue durée — sessions, narratives, historique, IntentHash."
parameters:
  archive: session_archive
  context: context_retention
  cleanup: memory_cleanup
  immutable: WAL + IntentHash
inherits: [absolute-rules-enforcement]
---
