---
name: github-integration
description: "Interfacage avec GitHub : creation de PR, merge, push, verification de remotes."
parameters:
  commands:
    - gh pr create
    - gh pr merge
    - git push
    - git remote -v
  safety_checks:
    - verify_remote
    - verify_branch
    - verify_ci
inherits: [absolute-rules-enforcement, gated-boot-sequence]
---
