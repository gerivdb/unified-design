---
name: morphism-maps-governance
description: "Cartes de transport causal entre repos : schema YAML + cycle de vie + regles de transport."
parameters:
  schema: MM-XXX
  lifecycle:
    - create
    - validate
    - transport
    - verify
  governance: HITL
inherits: [design-schema, fpick-functor]
---
