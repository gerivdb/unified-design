---
name: ontology-audit
description: "Audit ontologique pour verifier la coherence des connaissances entre les strates N, N+1, N+2. Couverture cible >95% sur BRAIN-DOCS."
parameters:
  strata:
    - N
    - N+1
    - N+2
  coverage_target: 95
  max_time_ms: 30000
  sources:
    - BRAIN-DOCS
    - ONTOLOGY
inherits: [stratified-repository-registry]
---

# Ontology Audit Atom

Pattern d'audit ontologique entre strates.

## Role

Verifie la coherence des connaissances entre les strates N, N+1, N+2 :
- **Strates** : N, N+1, N+2
- **Couverture cible** : >95% sur BRAIN-DOCS
- **Temps max** : 30s
- **Sources** : BRAIN-DOCS, ONTOLOGY

## Parametres

| Parametre | Type | Defaut | Description |
|-----------|------|--------|-------------|
| `strata` | list | `[N, N+1, N+2]` | Strates auditees |
| `coverage_target` | int | `95` | Couverture cible (%) |
| `max_time_ms` | int | `30000` | Temps max d'audit |
| `sources` | list | `[]` | Sources de connaissances |

## Invariants

- La couverture DOIT etre >95% sur BRAIN-DOCS
- L'audit DOIT etre execute dans le pipeline CI
- Les incoherences DOIVENT etre corrigees avant merge

## Exemple d'usage

```yaml
capabilities:
  - name: ontology-audit
    parameters:
      strata: [N, N+1, N+2]
      coverage_target: 95
      max_time_ms: 30000
      sources: [BRAIN-DOCS, ONTOLOGY]
```

## References

- TINA (scripts/ontology_audit_brain_docs.ps1)
- BRAIN-DOCS
