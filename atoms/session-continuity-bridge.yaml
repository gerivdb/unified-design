---
name: session-continuity-bridge
description: "Bridge de continuité inter-sessions via WAL + stashes + manifests."
parameters:
  continuity_mechanisms:
    - WAL append-only
    - git stash
    - session manifests
  reconstruction: supported
inherits: [artifact-synced-loop, scoped-loop]
---
