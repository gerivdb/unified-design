---
atom_id: ATOM-CONVERSATION-ANCHORING
name: conversation-anchoring
description: "Principe anti-hallucination pour méta-designer : ancrage conversationnel obligatoire, marquage [HYPOTHÈSE] pour extrapolations."
version: "1.0.0"
status: proposed
layer: L0
intent_hash: 0xATOM_CONVERSATION_ANCHORING_20260920
inherits: []
capabilities:
  - name: anti_hallucination_gate
    description: "Bloque la génération d'artefacts non ancrés."
    parameters:
      min_confidence: 0.6
      marking_required: "[HYPOTHÈSE]"
      levels:
        - explicite
        - implicite
        - manquant
governance:
  mandatory_for:
    - meta_designer
    - conversation_forensics
    - design_extraction
  linked_atoms:
    - ATOM-CONFIDENCE-THRESHOLD
    - ATOM-ARTIFACT-EXTRACTION-STATUS
    - ATOM-INDEPENDENT-SOURCES-RULE
  blocks:
    - phantom_path
    - ghost_artifact
    - hallucinated_design
---

# ATOM-CONVERSATION-ANCHORING

> **IntentHash** : `0xATOM_CONVERSATION_ANCHORING_20260920`  
> **Statut** : proposed  
> **Dépôt** : `gerivdb/unified-design`  
> **Màj requise si** : statut ADR passe à deprecated ou superseded  

---

## Description

Principe anti-hallucination pour tout méta-designer ou agent opérant sur des conversations : ne fabrique pas d'éléments non ancrés ; toute extrapolation doit être marquée `[HYPOTHÈSE]`.

## Règles

1. **Ancrage obligatoire** : Tout artefact proposé doit être rattachable à un passage explicite ou implicite de la conversation source.
2. **Marquage [HYPOTHÈSE]** : Toute extrapolation au-delà de l'ancrage doit être explicitement marquée.
3. **Distinction 3 niveaux** :
   - `explicite` : Mentionné clairement dans la conversation
   - `implicite` : Déductible par logique directe depuis des éléments explicites
   - `manquant` : Absent de la conversation ; nécessite HITL pour être confirmé
4. **Interdiction** : Présenter comme certain un élément `manquant` ou `hypothétique`.

## Structure causale

```
Cause : Conversation incomplète ou ambiguë
Mécanisme : Agent extrapole sans ancrage
Effet : Hallucination, artefact fantôme, dette de traçabilité
```

## Workflow

1. Lire la conversation / corpus source
2. Pour chaque artefact proposé :
   - Localiser le passage ancrant (citation + line référence)
   - Classer : explicite / implicite / manquant
   - Si implicite : documenter le saut logique
   - Si manquant : marquer `[HYPOTHÈSE]` + déclencher HITL
3. Joindre l'ancrage conversationnel dans la fiche artefact (champ 14)

## Intégration MDU

- Couplé à `meta-coherence` : détection GAP/GHOST/DRIFT/PHANTOM_PATH
- Couplé à `ATOM-ARTIFACT-EXTRACTION-STATUS` : statut `à confirmer` pour éléments non ancrés
- Couplé à `ATOM-CONFIDENCE-THRESHOLD` : seuil 0.6 minimum pour décision sans HITL

## Références

- ADR-2026-09-20-001 : Artifact Extraction MDU Extension
- PRD-MOC-ARTIFACT-EXTRACTION-20260920.md
- `meta-coherence` design

---

*Generated by governance-doc-writer skill — Pattern C*
