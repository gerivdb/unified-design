---
name: cron-centralized-registry
description: "Registre centralise cron avec schema YAML declaratif, NT Job Objects, mutex."
parameters:
  schema: ecos-sync-registry.yaml
  platform: windows
  mutex: true
  declarative: true
inherits: [scheduled-loop, eecs-config]
---
