---
name: session-cluster-builder
description: "Clustering automatique de repos en sessions thematiques pretes a executer."
parameters:
  clustering_criteria:
    - domain
    - layer
    - dependency_graph
  output: session_manifest
  ready_to_execute: true
inherits: [ecos-root-registry, closed-loop-goal]
---
