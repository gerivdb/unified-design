# CI Template - DÉSACTIVÉ
# Les workflows GitHub Actions sont interdits dans l'écosystème gerivdb.
# Le CI doit être exécuté localement via KIVA-CLI / ECOS-CLI.
# Ce fichier est un placeholder pour empêcher la propagation automatique.