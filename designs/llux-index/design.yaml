---
name: LLUX-INDEX
version: "1.0.0"
status: active
strate: L3-CITIZENS
type: cognitive
intent_hash: 0xLLUX_INDEX_RUNNER_20260821
description: >
  LLUX-INDEX — Runner d'indexation LLUX. Indexe les incidents et les documents dans la base de connaissances.

dependencies:
    - supermemory
    - knowledge-graph-memory

consumers:
  - llux-match
  - llux-learn

capabilities:
  - name: primary-function
    description: Fonction principale du runner LLUX-INDEX
    parameters:
      input: string
    outputs:
      - result: object

constraints:
  - cpu_only: true
