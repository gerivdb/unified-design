---
name: DEPLOYEX
version: "1.0.0"
status: proposed
strate: L4-TOOLS
type: cognitive
intent_hash: 0xDEPLOYEX_DEPLOYMENT_RUNNER_20260821
description: >
  DEPLOYEX — Runner de deploiement multi-repo. Coordonne les deploiements across multiple repositories.

dependencies:
    - gericode
    - topos

consumers:
  - KIX
  - ECOS-CLI

capabilities:
  - name: primary-function
    description: Fonction principale du runner DEPLOYEX
    parameters:
      input: string
    outputs:
      - result: object

constraints:
  - cpu_only: true
